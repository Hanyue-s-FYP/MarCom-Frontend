export enum BadgeType {
  NEUTRAL = "NEUTRAL",
  WARN = "WARN", // actually is just yellow, warn sounds more suitable but it applies for RUNNING status as well
  SUCCESS = "SUCCESS", // actually is just green
  PRIMARY = "PRIMARY", // u guessed it, primary color
}
